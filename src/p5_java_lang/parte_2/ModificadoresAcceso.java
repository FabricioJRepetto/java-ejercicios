package p5_java_lang.parte_2;

public class ModificadoresAcceso {
    /*
     Modificadores de acceso:
     Delimitan el alcance del objeto dentro del proyecto.

     • public:
       (Proyecto)
       Visible desde cualquier lugar del proyecto.

     • protected:
       (Paquete + Herencia)
       Visible dentro del paquete y para clases hijas fuera del mismo.

     • default:
       (Paquete)
       Solo es visible dentro del mismo paquete.

     • private:
       (Clase)
       Atributos o métodos privados son accesibles solamente por la misma clase.
     */
}
